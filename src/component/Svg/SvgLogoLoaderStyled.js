import styled from 'styled-components';

import { variable } from '../../style/variable';

export const SvgLogoLoaderStyled = styled.svg`
    fill: ${variable.colorGray2};
    height: 100px;
    left: 50%;
    position: absolute;
    top: 50%;
    transform: translate3d(-50%, -50%, 0);
`;
